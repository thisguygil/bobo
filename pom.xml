<build>
    <plugins>
        <plugin>
            <groupId>com.heroku.sdk</groupId>
            <artifactId>heroku-maven-plugin</artifactId>
            <version>3.0.4</version>
            <configuration>
                <appName>${heroku.appName}</appName>
                <processTypes>
                    <web>java -jar build/libs/bobo-1.0-all.jar</web>
                </processTypes>
            </configuration>
        </plugin>
    </plugins>
</build>